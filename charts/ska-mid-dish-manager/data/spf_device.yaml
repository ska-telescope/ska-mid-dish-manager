name: "spf-device-{{.Release.Name}}"
function: test-device
domain: testing
command: "/usr/local/bin/SPFDevice "
server:
  name: "SPFDevice"
  instances:
  {{- range  .Values.coalesced_dishes }}
    - name: "{{ . }}"
      classes:
        - name: "SPFDevice"
          devices:
            - name: "mid_d{{ printf "%04s" . }}/spf/simulator"
  {{- end }}
depends_on:
  - device: sys/database/2
image:
  registry: "{{.Values.dishmanager.image.registry}}"
  image: "{{.Values.dishmanager.image.image}}"
  tag: "{{.Values.dishmanager.image.tag}}"
  pullPolicy: "{{.Values.dishmanager.image.pullPolicy}}"

